# FrugaRelay (iOS)

Swift package for the Fruga Relay iOS SDK: a `WKWebView` plus JSON bridge that loads the Fruga CDN shell page and presents the Relay screen.

CI for this package runs in the public mirror repo (`IOS_MIRROR_REPO` in Actions variables).
